name: Continuous Deployment For Production

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  verification:
    name: Check Merge Conflict
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Check for Merge Conflicts
        run: |
          git fetch origin main
          if ! git merge-base --is-ancestor origin/main HEAD; then
            echo "🚨 Konflik terdeteksi! Perbaiki sebelum merge."
            exit 1
          fi
          echo "✅ Tidak ada konflik. Lanjutkan."
          
      - name: Detect Merge Conflict Markers
        run: |
          CHANGED_FILES=$(git diff --name-only ${{ github.event.pull_request.base.sha }} ${{ github.sha }} | grep -E '\.(php|js|html)$' || true)

          if [[ -z "$CHANGED_FILES" ]]; then
            echo "✅ Tidak ada file PHP, JS, HTML yang diubah."
            exit 0
          fi

          echo "📂 File yang diubah:"
          echo "$CHANGED_FILES"

          echo "$CHANGED_FILES" | xargs grep -Hn "<<<<<<< HEAD" && {
            echo "🚨 Konflik terdeteksi! Selesaikan sebelum merge."
            exit 1
          } || echo "✅ Tidak ada konflik terdeteksi."
          
  deployment:
    name: Deploy to Server
    needs: verification
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Deploy to Production Server
        run: |
          echo "✅ Success"
